# Restart

重启远程服务器上的后端服务。

## 执行步骤

```bash
cd f:/uniapp-project/deploy && node cli.js restart
```

## 说明

使用 PM2 重启后端 Node.js 服务，不会影响前端 Nginx 服务。

## 相关命令

- `/status` - 查看服务状态
- `/logs` - 查看日志
- `/stop` - 停止服务
